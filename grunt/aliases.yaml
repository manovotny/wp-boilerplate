default:
  - build

build:
  - lib
  - tests
  - php
  - css
  - js

config:
  - replace

css:
  - clean:css
  - scsslint
  - sass
  - autoprefixer
  - cssmin

js:
  - clean:js
  - jslint
  - concat
  - uglify

lib:
  - clean:lib
  - copy

php:
  - phplint

tests:
  - phpunit

#manual:
#  - shell
#  - watch