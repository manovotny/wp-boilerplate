default:
  - build

init:
  - config
  - shell:install
  - update
  - build

config:
  - replace
  - rename

build:
  - clean:css
  - clean:js
  - lint
  - test
  - concat
  - sass

generate:
  - autoprefixer
  - cssmin
  - makepot
  - concat
  - uglify

update:
  - clean:lib
  - shell:update
  - copy
  - curl
  - build

lint:
  - jshint
  - jslint
  - phplint
  - scsslint

test:
  - phpunit

release:
  - build
  - replace:project_version
  - generate
